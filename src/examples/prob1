// Multiples of 3 or 5
int sum = 0
int sumAll = 0
int res = 0
int i = 0
while (i < 1000)
	if (i % 3 == 0)
		sum += i
		if (i % 5 == 0)
	    	sumAll += i
		endif
	endif
	if (i % 5 == 0)
	    sum += i
	endif
	i += 1
endwhile
res = sum - sumAll
print(res, int)